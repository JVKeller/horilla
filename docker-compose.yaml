services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8090:8090
    restart: unless-stopped
    environment:
      DATABASE_URL: "postgres://postgres:postgres@db:5454/horilla"
    command: ./entrypoint1.sh
    depends_on:
      db:
        condition: service_healthy

  db:
    image: postgres:16-bullseye
    environment:
      POSTGRES_DB: horilla
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - 5454:5454
    restart: unless-stopped
    volumes:
      - horilla-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  horilla-data:
